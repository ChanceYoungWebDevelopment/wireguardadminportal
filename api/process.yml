apps:
  - script: './index.js'
    exec_mode: 'fork'
    name: 'wgadminapi'
    env:
      WGA_POSTGRES_USER: "postgres"  
      WGA_POSTGRES_HOST: "localhost"
      WGA_POSTGRES_DB: "postgres"
      WGA_POSTGRES_PASSWORD: "password"
      WGA_PORT: 3500